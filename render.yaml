 services:
  - type: web
    name: credit-risk-api
    runtime: python
    buildCommand: pip install -r requirements.txt && python main.py
    startCommand: uvicorn app.main:app --host 0.0.0.0 --post 10000


  